# User Programs and Shell Environment

Cody's shell, terminal, browser, XDG, and user program details are owned locally by [`users/cody/README.md`](../users/cody/README.md).

Start there for the current implementation map, especially:

- `users/cody/core.nix`
- `users/cody/desktop/programs.nix`
- `users/cody/desktop/xdg.nix`
- `users/cody/desktop/packages/scripts/`

This central page is only an index pointer. Keep implementation detail next to the user configuration so the operator editing the files has the live context.
