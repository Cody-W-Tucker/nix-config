# Custom packages

Package-owned documentation now lives next to the code:

- [packages/README.md](../packages/README.md)

Use that README for the current package layout, system scripts, and local package notes for `gh-star-search`, `en-core-web-sm`, and `kokoro`.

This page stays as the central index entry so older docs links still have a landing place. Do not add implementation details here; keep them in `packages/README.md` or the package directory that owns them.
