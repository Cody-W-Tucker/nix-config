# Hyprland Window Manager

Hyprland system enablement is in `modules/desktop/hyprland.nix`; Cody's user-level Hyprland behavior is owned by [`users/cody/README.md`](../users/cody/README.md).

Primary local files:

- `users/cody/desktop/hyprland.nix`
- `users/cody/desktop/hyprland/settings.nix`
- `users/cody/desktop/hyprland/autostart.nix`
- `users/cody/desktop/speech-to-text.nix`

Use the local README for keybinding, workspace, idle/lock, hardwareConfig, and dictation edit guidance. This page intentionally does not duplicate those sections.
