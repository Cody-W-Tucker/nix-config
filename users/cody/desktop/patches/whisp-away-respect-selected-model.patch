diff --git a/src/faster_whisper/client.rs b/src/faster_whisper/client.rs
index f602d9b..859af14 100644
--- a/src/faster_whisper/client.rs
+++ b/src/faster_whisper/client.rs
@@ -72,7 +72,7 @@ pub fn stop_and_transcribe_daemon(socket_path: &str, use_clipboard: bool) -> Res
                 ])
                 .spawn()?;
             
-            let result = transcribe_with_faster_whisper(&audio_file, "base.en", use_clipboard);
+            let result = transcribe_with_faster_whisper(&audio_file, &model, use_clipboard);
             
             let _ = fs::remove_file(&audio_file);
             
