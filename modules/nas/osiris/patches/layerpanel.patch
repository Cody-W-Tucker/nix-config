--- a/src/components/LayerPanel.tsx
+++ b/src/components/LayerPanel.tsx
@@ -6,7 +6,7 @@ import {
   Plane, Satellite, Sun, AlertTriangle, Camera,
   CloudLightning, Ship, Network, Database, Ghost,
-  Flame, Tv, Radio, Mountain, Anchor, Megaphone
+  Flame, Tv, Radio, Radar, Mountain, Anchor, Megaphone
 } from 'lucide-react';
 
 interface LayerPanelProps {
   data: any;
@@ -40,11 +40,22 @@ const LAYER_GROUPS: LayerGroupDef[] = [
-  {
-    label: 'SDK',
-    fullLabel: 'OSIRIS SDK',
-    icon: Network,
-    layers: [
-      { key: 'sdk_sea', label: 'Maritime Lines', dataKey: 'sdk_entities' },
-    ],
-  },
+  {
+    label: 'ORACLE',
+    fullLabel: 'PYTHIA FORECAST',
+    icon: Radar,
+    layers: [
+      { key: 'predictions', label: 'Forecast Rings (7 Days)', dataKey: '' },
+      { key: 'predictions_all', label: '+ Month / Year Rings', dataKey: '' },
+    ],
+  },
+  {
+    label: 'LATTICE',
+    fullLabel: 'COMMS & INTEL LATTICE',
+    icon: Network,
+    layers: [
+      { key: 'sdk_sea', label: 'Submarine Cables', dataKey: 'sdk_entities' },
+      { key: 'sdk_air', label: 'Air Corridors', dataKey: 'sdk_entities' },
+      { key: 'sdk_naval', label: 'Naval Intel Mesh', dataKey: 'sdk_entities' },
+    ],
+  },
   {
     label: 'AVIATION',
     fullLabel: 'AVIATION',
@@ -94,5 +106,8 @@ const LAYER_GROUPS: LayerGroupDef[] = [
       { key: 'earthquakes', label: 'Earthquakes', dataKey: 'earthquakes' },
       { key: 'fires', label: 'Active Fires', dataKey: 'fires' },
       { key: 'weather', label: 'Severe Weather', dataKey: 'weather_events' },
+      { key: 'volcanoes', label: 'Volcanoes (USGS)', dataKey: 'geohazards' },
+      { key: 'hurricanes', label: 'Hurricane Cones (NHC)', dataKey: 'hurricanes' },
+      { key: 'flood', label: 'Flood Outlook (30d)', dataKey: 'flood' },
     ],
   },
   {
@@ -134,6 +149,18 @@ const LAYER_GROUPS: LayerGroupDef[] = [
       { key: 'terrain_3d', label: '3D Terrain & Buildings', dataKey: '' },
     ],
   },
+  {
+    label: 'SOCIAL',
+    fullLabel: 'SOCIAL & ECONOMIC',
+    icon: Megaphone,
+    layers: [
+      { key: 'displacement', label: 'Displacement / Refugees', dataKey: 'displacement' },
+      { key: 'health_outbreaks', label: 'Disease Outbreaks', dataKey: 'health_outbreaks' },
+      { key: 'economy', label: 'Inflation / Cost of Living', dataKey: 'economy' },
+      { key: 'censorship', label: 'Internet Censorship', dataKey: 'censorship' },
+      { key: 'unrest', label: 'Civil Unrest', dataKey: 'unrest' },
+      { key: 'food_security', label: 'Food Insecurity', dataKey: 'food_security' },
+      { key: 'unemployment', label: 'Unemployment', dataKey: 'unemployment' },
+      { key: 'gdp_growth', label: 'GDP Growth', dataKey: 'gdp_growth' },
+      { key: 'poverty', label: 'Extreme Poverty', dataKey: 'poverty' },
+    ],
+  },
 ];
